services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: habitgraph
      POSTGRES_PASSWORD: habitgraph
      POSTGRES_DB: habitgraph
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
